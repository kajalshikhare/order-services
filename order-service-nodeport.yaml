apiVersion: v1
kind: Service

metadata:
  name: order-service-deployment
  namespace: ecommerce-dev

spec:
  type: NodePort   # REQUIRED

  selector:
    appname: order-service-deployment

  ports:
    - port: 8083
      targetPort: 8083
      nodePort: 30003