apiVersion: v1
kind: Service

metadata:
  name: postgres-headless-order
  namespace: ecommerce-dev

spec:
  clusterIP: None   

  selector:
    app: order-postgres

  ports:
  - port: 5432
    targetPort: 5432